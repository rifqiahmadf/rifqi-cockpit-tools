# Backend configuration
PORT=3001
DB_PATH=./server/data/cockpit.db
NODE_ENV=development

# Frontend configuration (set during build)
VITE_API_BASE_URL=/api

# Optional: Custom data directory
# DATA_DIR=/path/to/data
